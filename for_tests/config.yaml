# Конфигурационный файл с уязвимостями
database:
  host: localhost
  port: 5432
  username: admin
  # Уязвимость: пароль в конфигурационном файле
  password: "PostgresAdminPass123!"
  # Уязвимость: секретный ключ
  secret_key: "my-secret-key-1234567890abcdef"

api:
  # Уязвимость: API ключи в конфиге
  stripe_secret_key: "sk_live_abcdefghijklmnopqrstuvwxyz"
  aws_access_key: "AKIAIOSFODNN7EXAMPLE"
  aws_secret_key: "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"

jwt:
  # Уязвимость: JWT секрет в конфиге
  secret: "jwt-super-secret-1234567890"
  algorithm: "HS256"

security:
  # Уязвимость: отключение проверок
  ssl_verify: false
  cors_allowed_origins: "*"
  rate_limit: false

dependencies:
  # Уязвимость: устаревшие версии
  flask: "1.1.2"
  django: "2.2.8"
  requests: "2.20.0"
  pyyaml: "5.1"
  urllib3: "1.25.0"

server:
  # Уязвимость: небезопасные настройки
  debug: true
  host: "0.0.0.0"
  port: 5000
